# Выпуск v0-2-6

Дата: 2026-09-23. Исходный коммит `a32b2ac53dd6444def160f5a192475b3316e8254` опубликован в `origin/main` репозитория `Lainterus1/HighGrade`. Перед отправкой из изолированного архива этого коммита была собрана и локально активирована пользовательская поставка v0-2-6; `global-status` подтвердил её после переключения. Это местная активация, а не развёртывание целевого проекта.

До коммита пройдены `cargo fmt --all -- --check`, 69 Rust-тестов, `cargo build --release --locked`, структурная проверка репозитория, проверка хешей `kit/` и строгая валидация OpenSpec. Исторический [отчёт проверки кандидата](../reviews/deploy-publication-consent-review.md) фиксирует состояние до коммита и активации; его границу не следует читать как текущий статус выпуска.

Откат местной установки: сохранённый предыдущий релиз через `global-update --rollback v0-2-5` с последующим `global-status`. Откат не меняет уже опубликованный Git-коммит. Практические пилоты на SNAF и других целевых проектах не проводились; пользовательская приёмка их workflow не заявлена.
