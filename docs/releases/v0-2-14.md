# Поставка v0-2-14

Автор принял HG-0002, HG-0003 и HG-0004 и разрешил локальную активацию, синхронизацию документации и публикацию в origin/main. Точная запись человеческих решений привязывается к проверенной редакции через spec-decide.

Проверено: 113 тестов passed, 0 skipped; независимое REVIEW_GO; изолированный полный цикл установленной CLI; doctor/bootstrap/repository passed; OpenSpec strict 7/7. [Отчёт](../reviews/spec-catalog.md). Старые OpenSpec-файлы не изменялись.

## Активация и публикация

Состояние: локально активирована, global-status passed. HG-0002/3/4: ready/accepted; решения автора записаны с датой через spec-decide и привязаны к исходному коммиту.

- Исходный коммит: `2bf93b6ea54ebd717adfcf1d4f56ed6535f5ce73`. CLI собрана cargo build --release --locked из отдельного git archive этого SHA.
- candidate_sha256: `eb1ec3de2f7fe9aac97977f33f3fb098b690f0052895784ec59e7d5b4ff131f4`.
- manifest_sha256: `8ae8fce4a825724697bfd6240fd1e4826f58f529a305bf05e78400f2ba344288`.
- Preview и apply использовали один fingerprint; apply passed, active_release=v0-2-14, установленный global-status passed.
- Документальные изменения и человеческие решения после исходного SHA не изменяют kit или CLI и не требуют повторной активации.

Поставка и документация опубликованы обычным push в origin/main; подтверждён удалённый SHA `cfd3607f9fc3d1e956a7b46c72238929bf7f9494` (диапазон от a4de578). Последующий документальный коммит сохраняет эту запись. CI нового снимка при первичной проверке ещё не появился; успешный статус предыдущего запуска не относится к этому выпуску.

Обычный push запускает Verify source без развёртывания. Общая активация не изменяет подключаемые проекты и отдельные плагины.
