# Выпуск v0-2-7

Дата: 2026-09-24 (Asia/Bangkok). Исходный выпуск `b750994db707050b7223b92491171f4eede6745f` опубликован в `origin/main` репозитория `Lainterus1/HighGrade` и активирован локально как v0-2-7. Последующий коммит `74181b6c82b681f4f8c5a98aafe3613f5ac81f0c` исправил только правила LF для старых хешируемых файлов в Windows CI; он не менял программу или состав поставки.

## Состав и проверки

Разделены `highgrade-approve` для локального принятия задачи и `highgrade-push` для отправки диапазона коммитов. Сохранён `highgrade-update`; миграция с v0-2-6 и откат проверены на изолированных профилях. OpenSpec-изменение [архивировано](../../openspec/changes/archive/2026-09-23-approve-push-workflow/tasks.md). Независимое ревью и локальные проверки зафиксированы в [отчёте](../reviews/approve-push-workflow-review.md). Изолированная сборка исходного SHA установилась в тестовый профиль как v0-2-7.

## Фактические этапы

- Локальная активация: `global-update` из `git archive` коммита `b750994`; preview и apply использовали один `candidate_sha256`, установленный `global-status` подтвердил v0-2-7 и сохранение v0-2-6 для отката. Новые `approve` и `push` принадлежат активному журналу, старый маршрутизатор удалён.
- Удалённая публикация: push исходного SHA подтвердил `origin/main=b750994`. После исправления CI повторный push подтвердил `origin/main=74181b6` на момент проверки.
- CI: первый прогон `35895976341` выявил несовпадение хеша старой тестовой фикстуры из-за CRLF в Windows checkout. После закрепления LF отдельный checkout с `core.autocrlf=true` прошёл 30 затронутых тестов; повторный GitHub Actions `35896602724` успешно выполнил форматирование, все Rust-тесты, релизную сборку, проверку структуры и OpenSpec. CI не активирует установку и не развёртывает проекты.
- Пилоты SNAF и других проектов: не проводились; пользовательская приёмка их workflow не заявлена.

Откат локальной установки: `global-update --rollback v0-2-6`, затем `global-status`. Откат не отменяет Git-коммиты. Эта запись добавлена после исходного выпуска и не изменяет установленный бинарный артефакт.
