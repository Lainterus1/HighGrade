# Выпуск v0-2-13

CLI 0.2.13. Автоматические номера спецификаций, title и дата создания, JSON-сводка прогресса, история человеческой приёмки и явная миграция v1 → v2 с резервной копией. Авторские документы следуют языку проекта; в Проекте High Grade выбран русский. Старые OpenSpec-материалы сохраняются.

## Решение автора и область

2026-09-24 автор написал: «Отлично, принимаю спеки, активируем поставку, отправляем в git и актуализируем документацию».

Приняты требования HG-SP-R1–R5 из HG-SPEC-PROGRESS и уточнение русского языка. Разрешены коммит текущего состава, местная активация из его SHA и обычный push в origin/main. Повторная проверка неизменённых требований ведётся в HG-0001; архивная HG-SPEC-PROGRESS сохраняет прежние доказательства. Пользовательская проверка внешнего приложения этим решением не утверждается.

## Проверки

Актуальная проверка: 96/96 nextest, 25/25 автоматических сценариев прежнего каталога, 7/7 OpenSpec, выборочный контекст на 100 и 200 спеках, структура и импорт 54 файлов — успешно. Doctor — passed; inspect не содержит failed/unknown. Исходный [JUnit](../evidence/spec-progress/nextest.xml) сохранён, входы закреплены в evidence HG-0001. Полный cargo test ранее проверенного неизменённого кода также прошёл. Независимое ревью реализации, языка и состава выпуска: REVIEW_GO. История — в [отчёте](../reviews/spec-progress.md).

## Локальная активация

- Исходный коммит: `2fa8189e8422abd237bed697313e9d2e0bfe9595`. CLI собрана через cargo build --release --locked из его отдельного git archive; активировался этот бинарник и kit из того же SHA.
- Переход: v0-2-12 → v0-2-13.
- candidate_sha256: `b0e122c9a3a085afe0d4b413ef2a143b26fa4bb0880be246aafe37c8e60526e8`.
- manifest_sha256: `26e83174e4f79694a08c8f9a06c545aa7479ec4b86b9b59f503eb8383340b5d6`.
- Предварительная проверка потребовала смыслового ревью; изменения сверены с REVIEW_GO и разрешением автора. Применение и установленный global-status: passed, без предупреждений очистки. Установленный doctor: passed.
- Установленный spec-list: HG-0001 — technical=ready, human=accepted. HG-SPEC-PROGRESS сохраняет прежний снимок и устаревшие доказательства; архив не переписывался.

## Публикация и ограничения

Документальные коммиты после исходного SHA фиксируют фактическую активацию; повторное переключение Поставки для них не требуется. Цель публикации — origin/main, обычный push. Он запускает [Verify source](https://github.com/Lainterus1/HighGrade/actions/workflows/verify.yml) без развёртывания; удалённый SHA и CI проверяются отдельно.

Известные ограничения: общий бюджет контекста превышен, внешние ссылки inspect не проверяет. Бюджет не повышался для скрытия предупреждения. Приёмка спецификаций не утверждает пользовательскую проверку внешнего приложения. SNAF, другие проекты и профильные плагины не менялись.
