# Выпуск v0-2-9

Дата: 2026-09-24 (Asia/Bangkok). Исходные коммиты `aed37c1b5e182f49c81b9c7b8c7598b4e7e04c2a` и `4133c3662c65931f7f778f06233733c4348977ca` опубликованы в `origin/main`. Второй коммит сохранил совместимость установленного маршрутизатора `highgrade-update` с v0-2-8. Ранее оставшиеся локальными изменения `Cargo.toml` и `docs/DEVELOPMENT.md` вошли в первый коммит.

Локальная активация выполнена из изолированной копии `4133c36`: preview и apply использовали один `candidate_sha256`, `global-status` подтвердил v0-2-9. Собственные файлы старых выпусков удалены; эксплуатационные проекты не менялись.

Проверки кандидата: 77/77 Rust-тестов, 15/15 автоматических сценариев, строгая проверка пяти OpenSpec-спек, release-сборка и установка в изолированном профиле. Независимое ревью выявило ошибку повторной очистки, исправленную до принятия. [GitHub Actions Verify source](https://github.com/Lainterus1/HighGrade/actions/runs/35959468173) проверяет отправленный SHA; на момент составления записи прогон ещё выполнялся.

Ограничения: 37 ручных сценариев не объявлены автоматически проверенными. Полный маршрут контекста превышает рабочий бюджет; выборочная проверка затронутой области не превышает его. Пилоты целевых проектов и пользовательская приёмка не проводились.
