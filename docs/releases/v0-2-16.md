# Поставка v0-2-16

Выпуск сопровождает [HG-0019](../../specs/changes/HG-0019/spec.json): каталог `target/` Проекта получил документированную структуру, явное безопасное обслуживание и проверенный жизненный цикл кандидата. Правила и навыки переносимой Поставки не изменились; в `kit/manifest.json` изменены только `release` и `cli_version`. Новый ID нужен для неизменяемой локальной активации бинарника из нового коммита.

## Проверка и активация

- Исходный коммит кандидата: `d67fe096f3752499af2cf30311870caf376deaa0`.
- `candidate_sha256` в preview и apply: `12bcd3638a212f3f4a91b004d2babb697c555a2d9b59e36192e5f191a384b1cf`.
- `manifest_sha256`: `bd18057411bb7519ed9832917c6ec7319c2a54b191b1ed9955eb9fbb1aa99ca5`; SHA256 установленной и кандидатной CLI: `1bbea4d0cffb8e65fa37c1181e8bdefe96cefa67ee5008042b645d521f13861b`.
- Preview потребовал смысловой сверки; все 26 файлов манифеста остались прежними. Apply завершился `passed`, затем `global-status` подтвердил `v0-2-16` и целостность установки.
- `cargo test --locked`, Nextest 116/116, Python 12/12, проверка структуры/хешей Поставки и связь 38/38 автоматических сценариев прошли. 54 сценария не подтверждены через `trace`; три новых сценария HG-0019 имеют отдельный Python-отчёт и `spec-check: passed`. `inspect` оставил только 9 предупреждений о внешних ссылках. После обслуживания `target/` занимает 176,65 МиБ без неизвестных путей и ссылок.

Технический статус HG-0019 — `ready`; решение человека по этой редакции не записано. Отправка в Git и CI проверяются отдельно от локальной активации.
