# Проверка самостоятельной Поставки

Статус: технически завершено по [плану](../plans/2026-09-24-portable-delivery.md). Публикация, общая активация и пользовательская приёмка не выполнялись. Основные машинные результаты находятся в `target/portable-delivery/`; учебная среда — `%TEMP%/highgrade-portable-20260924`. Это локальные результаты, не опубликованные артефакты.

## P1 — самостоятельность

- OpenSpec 1.13.1: `validate self-contained-delivery --strict --no-interactive` прошёл до реализации.
- `cargo test --locked --test global_contracts`: 18/18 на кандидате 0.2.10, включая `shipped_registry_example_is_accepted_without_inventing_budgets`. Проверяются настоящие install/status/inspect; содержательность навыка ими не доказывается.
- HG-PD-S01: установка, удаление исходной временной копии и чтение установленного выпуска/ссылок проверены Rust-тестом.
- HG-PD-S02/S03: отдельный агент адаптировал два каталога через установленный пакет. Сохранил исходный `foreign.txt`, предметный инвариант и принятое исключение, не запросил повторного разрешения. Неизвестные команды не выдуманы.
- Найден дефект: без примера JSON агент записал бюджеты строкой `unknown`, что дало `BudgetInvalid`. Исправление: пример формата в поставляемом CLI-справочнике и ссылка из init; настоящий inspect принимает пример и оставляет `BudgetNotAgreed`. Повторное агентное упражнение завершено в `p1-fixed.log`: оба bootstrap прошли, inspect вернул только BudgetNotAgreed, foreign.txt сохранил SHA256 636461c3423afef470e7c85d3d5eec17077bbf28fd5c7f3b49a7c3b842366f81.
- HG-PD-S04: отдельный агент правильно разобрал выбор действия для неприменимой активации и ошибки с неизвестным восстановлением. Это проверка решения по инструкции; фактический commit и запуск проектной активации не выполнялись.
- Независимое `codex review --uncommitted`: замечание P2 о непересмотренном бюджете (106716/94000 и README 6576/6500). README сокращён до 6469. Общие правила вынесены из GLOBAL/ENGINEERING; маршрут уменьшен до 100313. Пересмотр не повышает потолок: остающееся превышение связано с действующими спеками и временными артефактами последовательного плана; следующий замер после архивирования P1 и после P6. Это предупреждение, а не успешная проверка бюджета; смысл требований ради лимита не удаляется.

## Границы доказательства

Один агентный прогон не устанавливает статистическую надёжность. Исходная проверка P1 использовала промежуточный изолированный пакет под именем v0-2-9; исправленный и последующие кандидаты имеют отдельное имя v0-2-10-candidate. Хеши и результаты установки остаются в машинных записях. Финальное упражнение P4/P6 относится к окончательному кандидату.

SNAF baseline: рабочее дерево стало чистым вне этого исполнения (HEAD 985f9fb не изменился); прежние .highgrade и план отсутствуют. `. artifacts/stable-sdd-bdd/use-toolchain.ps1; pnpm check:toolchain; pnpm test` — Node 24.18.0 / pnpm 11.17.0, 42/42 теста прошли; журнал snaf-baseline-tests.log. Продуктовый код не менялся.

## P2 — сценарии, доказательства и продолжение

- HG-PD-S06/S07/S08/S09: отдельный агент продолжил fixture `behavior` через установленный `profile-p2`, сохранил одно tasks.md и принятые решения. Нашёл пропущенный сценарий и не использовал старый отчёт для изменившегося кода; включённая проверка сначала обнаружила реальную порчу сохранённого объекта, затем исправлена функция catalog.mjs.
- `node --test --test-reporter=tap catalog.test.mjs`: 2 passed, 0 failed, 0 skipped. Native TAP принят как автоматическое доказательство вне trace. Команда, время, Node 24.15.0, хеши входов и отчёта записаны в fixture `evidence/current.md`; предыдущий отчёт сохранён. Сторонний файл не изменён. Агент оставил обязательное ревью следующим шагом, а не заявил его выполненным.
- Родитель отдельно проверил код и ожидаемые инварианты: валидация предшествует мутации, связанные данные и identity сохранены. Копия с исходной ошибкой даёт 1 failed/1 passed (`target/portable-delivery/p2-mutant.tap`); текущая реализация даёт 2/2. Граница — локальная функция, не внешнее хранилище.
- Независимое ревью процедур (`p2-review.log`) не нашло существенных дефектов. Оно не подменяет выполненное упражнение. Структурная проверка и diff --check прошли; новых навыков и зависимостей нет.

## P3 — совместимость

- HG-PD-S10/S11/S12: `cargo test --locked --test global_contracts` — 20/20. Поддерживаемый заголовок даёт только schema-only/not_assessed; неизвестная схема и отсутствие заголовка отвергаются без записи; global-update сохраняет байты местной инструкции с исключением в проекте внутри профиля. Release-сборка 0.2.10 прошла.
- HG-PD-S13: отдельный агент работал через profile-p3 только на чтение. Неизвестная схема 99 не понижена; отсутствующая обязательная проверка и конфликт local-run названы явно; принятое исключение активации сохранено; публикация не выполнялась. Целевые файлы остались неизменными.
- В агентном отчёте найдено неточное указание exit=1 для JSON unknown: код оболочки принят за код CLI. Родитель повторил ту же диагностику с немедленным захватом LASTEXITCODE: фактический код 2, JSON unknown (`p3-doctor-missing.json`). Исправление в справочнике: карта кодов и требование отделять CLI от оболочки. Поведенческие ограничения агент определил правильно; исходный отчёт сохранён как история, его код 1 не используется в итоговом доказательстве.
- Независимое ревью P3 (`p3-review.log`) существенных дефектов не нашло. Проверка схемы не означает смысловую готовность; машинные тесты и агентное упражнение имеют разные границы.

## P4 — окончательный кандидат

- CLI 0.2.10, выпуск `v0-2-10-candidate`: `cargo fmt --all -- --check`, release build и 81/81 native nextest прошли. Trace подтвердил 20/20 автоматических сценариев; 45 ручных не включены в автоматическое доказательство. Все шесть итоговых OpenSpec-спек прошли strict validate.
- Изолированный `profile-final` установлен и проверен. SHA256 манифеста `ad776ea6d7fe2a942cf7914e55153caa59a57b8908a20c4f8fba17aa37ea0aec`, CLI `b45edfbb47e80d43befa56541687c0ef351ac7f1a99dad33548aa43761fccb9e`. Финальные bootstrap пустого/существующего fixtures прошли; inspect сохранил только известные несогласованные бюджеты.
- Общая установка пользователя не переключалась. Исходники, commit, публикация и приёмка остаются отдельными состояниями.

## P6 — SNAF

Адаптация выполнена установленным кандидатом, без изменения runtime, зависимостей и прежних продуктовых спецификаций. Местные навыки сохраняют предметные роли; общих дублей нет. Канонические результаты пилота находятся в SNAF, `.highgrade/project/pilot.md` и OpenSpec `existing-snapshot-contract`; они не копируются сюда целиком.

Проверены native Vitest JSON (1 выбранный passed, 7 остальных skipped), полный pnpm test (42/42), pnpm check и четыре официальных skill validators. Инвентаризация сначала упёрлась в недоступный `.gstack`, затем в большой бинарник локального toolchain. Исключения записаны с причинами; выборочные evidence прочитаны отдельно. После этого inventory/doctor/bootstrap прошли, inspect оставил несогласованные бюджеты и внешние ссылки.

Первый процесс независимого ревью SNAF не смог запустить shell из-за `setup refresh had errors`; это ошибка среды, не REVIEW_GO. Ревью передано отдельному проверяющему текущей задачи с доступом на чтение.

Независимый `delivery_review` вернул REVIEW_GO: фактический diff и новые файлы проверены, четыре SHA256 совпадают с native evidence, логи прочитаны, внутренние ссылки разрешаются. Новый исполнитель нашёл следующий незавершённый шаг и переиспользовал принятые роли/отсутствие активации. Материальных P0–P2 нет. Это ограниченный пилот, не доказательство всей браузерной системы или статистической надёжности агентов.

OpenSpec-задача SNAF архивирована; созданная архиватором заглушка Purpose заменена, итоговый strict validate прошёл. Финальный pnpm check повторён после архива и прошёл. Inventory и внутренние ссылки проверены повторно; inspect содержит только шесть BudgetNotAgreed и восемь ExternalLinkUnchecked.

## Итоговый пересмотр контекста

После завершения пилота по отдельному поручению автора уточнён выбор проверки в `kit/procedures/spec.md`: определить воспроизводимую подготовку состояния, действие и наблюдение результата. Обновлён хеш в манифесте; проверены все хеши Поставки, структура и diff. Это редакционное уточнение существующего контракта сценария. Изолированный профиль и приведённые выше отпечатки относятся к версии до уточнения; повторный агентный пилот и переключение установки не выполнялись.

План и состояние завершены и исключены из обычного маршрута как история. Итоговый маршрут Проекта — 104028 байт при бюджете 94000: предупреждение сохранено. Общие повторяющиеся правила уже сокращены в GLOBAL/QUALITY/CONTEXT/ENGINEERING; оставшийся рост связан с новой действующей спецификацией Поставки. Удалять её требования ради числа нельзя. Лимит не повышался; следующее решение о размере — при согласовании кандидата или следующей задаче контекста. Это не блокирует подтверждённые независимые проверки и не является PASS бюджета.
