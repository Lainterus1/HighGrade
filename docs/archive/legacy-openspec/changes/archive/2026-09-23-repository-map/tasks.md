# Tasks

## 1. Команда карты

- [x] 1.1 Добавить безопасный обход одного корня с устойчивым порядком, учётом новых и скрытых файлов, исключениями и запретом перехода по ссылкам; проверить тестами Git- и не-Git-проекты, кэши и Windows junction.
- [x] 1.2 Добавить ограниченное распознавание README, известных манифестов, конфигураций тестов и CI с выводом только фиксированных категорий и путей; проверить тестом, что контрольные секреты и исходные команды не попадают в JSON.
- [x] 1.3 Подключить `map --root` к CLI и форматам `passed`/`unknown`/`failed`, установить документированные пределы; проверить контрактными тестами неверный корень, частичный обход и ограничение размера результата.

## 2. Применение и поставка кандидата

- [x] 2.1 Описать поля, поддерживаемые форматы, пределы и пример запуска в справочнике CLI; проверить пример на фикстуре и ссылки проектным скриптом.
- [x] 2.2 Обновить общие процедуры init, task, spec, work и clear, а также применимое описание маршрута и архитектуры; проверить, что карта не названа полным аудитом и не подменяет `inventory`, `doctor`, `inspect` или `trace`.
- [x] 2.3 Согласовать версию исходного кандидата и хеши `kit/manifest.json` с изменённым CLI и процедурами, не переключая пользовательскую установку; проверить сборку и установку кандидата в изолированном профиле.

## 3. Итоговая проверка изменения

- [x] 3.1 Выполнить `cargo fmt --all -- --check`, затронутые тесты, `cargo test --locked`, release-сборку, репозиторный скрипт и строгую валидацию OpenSpec; записать точные результаты и ограничения.
- [x] 3.2 Проверить сценарии карты и смысловую согласованность затронутых документов отдельным ревью; устранить найденное и повторить затронутые проверки до передачи результата.
