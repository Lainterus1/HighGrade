# Проверка уточнённого Update

Дата: 24.09.2026. Исходный HEAD `acc5322fb487937465291a7b27f15ba0b0f38469` плюс изменение процедуры, справочника и местного обещания восстановления. CLI не менялся; использован release-бинарник из изолированной копии исходного SHA `0320ea8559016aa89b5554960dc6d8e0868de61a`.

## Наблюдаемые сценарии

Временная среда: `%TEMP%/highgrade-update-check-9a49d840738a4046a37739f9382dbed0`. Скрипт и сводка: `target/portable-delivery/check-update-safety.ps1`, `update-safety-results.json` в Проекте. Данные сценариев — JSON внутри временной среды. Названия `update-safety-test-next/following` принадлежат только фикстурам. Процедура скопирована из явного снимка текущей правки; candidate manifest SHA256 `a498d2030c570d73091c7b9ff0a7c414943a6c5b27409f462426676b02554444`.

- HG-PD-S14: реальный isolated global-install v0-2-10, затем preview/apply нового тестового выпуска с одним candidate_sha256; status подтверждает update-safety-test-next. Исходный бинарник привязан к SHA; новый снимок меняет только материалы и тестовый ID.
- HG-PD-S15: повтор возвращает GlobalSameRelease; status успешен, manifest hash из journal и бинарник совпадают. Другой состав с тем же ID тоже возвращает GlobalSameRelease, но отличается manifest hash; успех не объявляется. Чужой файл KEEP сохранён.
- HG-PD-S16: изменённый после preview кандидат отклонён; свежий preview с чужим блокирующим rules.md приводит к ошибке stage. Сохранённый прежний CLI подтверждает прежний активный выпуск, чужой блокер не изменён. Это проверка сбоя до переключения, а не инъекция ошибки postcheck/неудачного восстановления. Для последних инструкция требует наблюдения фактического status, без обещания успеха.

Команды и исходы проверены исполнителем через настоящие CLI-процессы. Автономное выполнение всей процедуры свежим агентом не заявляется; отдельный проверяющий оценивает применение установленной инструкции к полученным состояниям.

## Gates и границы

81/81 штатных nextest-тестов прошли после правки; проверены хеши. Независимое ревью обнаружило P2: в справочнике оставалась безусловная гарантия при прерывании переключения. Заменено требованием проверить фактическое состояние; повторное ревью — REVIEW_GO. После архива: 81/81 native nextest, 20/20 automatic trace, шесть strict-valid OpenSpec-спек, структура/хеши/diff прошли. Три новых ручных сценария подтверждены описанными упражнениями отдельно от trace. Проверяющий независимо сверил установленные инструкции, происхождение CLI, одинаковые бинарники и правильные решения для всех пяти случаев. Основная пользовательская установка остаётся опубликованной v0-2-10; новый состав не активирован. Рабочий manifest пока сохраняет release ID: перед следующим выпуском нужен новый ID, подмена опубликованного v0-2-10 недопустима. SNAF не читался и не менялся.

Пересмотр бюджета: 106046 bytes > 94000; требуется пересмотр, не автоматическое повышение. Новый контракт добавляет различимые случаи Update; дублирование полного правила в справочнике заменено ссылкой, задачи архивированы. Лимит не повышен, предупреждение сохранено; следующий пересмотр при подготовке следующего выпуска. Известного конфликта или структурного unknown нет.
