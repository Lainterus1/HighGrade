# Tasks

## 1. Предварительная проверка структуры

- [x] 1.1 Добавить `inspect --bootstrap` без зависимости от реестра и вывести все семь обязательных путей с состоянием точного пути; проверить фикстурами пустого, нового и частично адаптированного проекта.
- [x] 1.2 Перенести в предварительную проверку безопасный ограниченный обход и поиск кандидатов по фиксированным признакам, сохранив исключения, скрытые и новые файлы; проверить фикстурами альтернативного имени, неполного обхода, некорректных исключений, junction и контрольного секрета.
- [x] 1.3 Добавить состояния шести обязательных Markdown-связей отдельно от наличия файлов; проверить фикстурами отсутствующей, старой, неразрешимой и корректной ссылки.

## 2. Контроль подключённого проекта и удаление карты

- [x] 2.1 Расширить обычный `inspect` проверкой пяти пар роль/канонический путь, проектной инструкции и обязательных связей без потери проверок бюджета и остальных ссылок; проверить контрактными тестами старого пути, неполного реестра и корректного проекта.
- [x] 2.2 Удалить публичную операцию `map` и неиспользуемые эвристики стека, входа, тестов и CI, сохранив нужный безопасный обход; проверить CLI-тестами, что `map` больше недоступна, `inspect --bootstrap` доступен, а `doctor`, `inventory` и `trace` не изменили контракт.

## 3. Процедуры и документы

- [x] 3.1 Обновить `init`, аудит и шаблоны: агент ищет содержание вне кандидатов, переносит или разделяет существующее и создаёт только действительно недостающее; проверить сценарии по тексту процедуры и согласованность обязательных ссылок в шаблонах.
- [x] 3.2 Обновить остальные затронутые общие процедуры, справочник CLI, политику D25, архитектуру и команды под канонические пути и новый режим; проверить ссылки и структуру через `node scripts/check-repository.mjs`.
- [x] 3.3 Зарегистрировать итоговую способность и документы в проектном реестре, обновить версию/манифест кандидата без активации у пользователя; проверить размеры, хеши и `inspect` исходного проекта.

## 4. Интеграционная проверка

- [x] 4.1 Запустить `cargo fmt --all -- --check`, `cargo test --locked`, release-сборку и строгую валидацию OpenSpec; записать точные результаты и ограничения.
- [x] 4.2 Установить кандидата в изолированный профиль и проверить `global-status`, bootstrap и обычный `inspect`; выполнить read-only прогон на SNAF и сопоставить структурные находки с ручным аудитом, не меняя SNAF.
- [x] 4.3 Провести независимое ревью CLI, процедур и новых сценариев, устранить найденные замечания и повторить затронутые проверки; сохранить результат ревью отдельно от пользовательской приёмки и публикации.
