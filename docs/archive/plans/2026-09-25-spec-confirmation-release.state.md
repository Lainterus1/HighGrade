# Состояние: подтверждение спецификаций и выпуск

Plan: docs/archive/plans/2026-09-25-spec-confirmation-release.md
Workspace: D:\my_projects\MyCodex
Observed: HEAD 2d7ddd1; рабочее дерево содержит незакоммиченные изменения нескольких предыдущих задач; активная установка v0-2-14.
Status: DONE
Current: завершено
Next: нет
Blockers: для разрешённого выпуска нет; принятые автором технические ограничения сохранены. CLI `spec-decide` не записал accepted для неготовых редакций. После смены версии `spec-list` показывал 1 ready, 14 in_progress, 0 accepted; решение автора сохранено отдельно без ложного PASS.

## Ход и доказательства

- 2026-09-25: пользователь остановил дальнейшие пилоты, явно принял все действующие спеки и поручил продолжить коммит, активацию и публикацию. Дополнительно поручил убрать лишние синтетические примеры и запустить Goal; Goal создан для этого плана. HG-0009 уже `spec-check: passed`; HG-0010 не хватает S2 и итогового ревью, HG-0011 — семи сценариев и итогового ревью. Остаток не превращать в `passed`.
- Решение автора записано в `docs/evidence/spec-verification/2026-09-25-human-decision.json` с точными хешами исходного снимка 15 спек. Удалены пять временных каталогов `target/` (семь синтетических проектов, update-пилот и старые примеры) и два незавершённых/непривязанных пакета доказательств HG-0011; после очистки прежний `spec-list` остался 13 ready / 2 in_progress.
- Для активации изменённой Поставки подготовлен v0-2-15: `Cargo.toml`, `Cargo.lock`, `kit/manifest.json`; тестовые фикстуры обновлены на пару v0-2-15 → v0-2-16. `cargo test --locked` passed, `cargo fmt` passed, `source-scenarios check` passed (50 требований, 89 сценариев, 38 автоматических), `check-repository` passed, `scripts/tests` 7/7. После версии часть прежних входных отпечатков устарела; новые агентные пилоты по решению автора не запускаются.
- Итоговый прогон: Nextest 116/116, `source-scenarios` 38/38 автоматических при 51 неподтверждённом ручном, импорт 54 файла, архив 86 OpenSpec + 124 native, scale 100/200, inspect без failed/unknown. Независимый ревьюер выявил CI no-op (фильтр archived) и жёсткую ссылку на CLI 0.2.14; исправлено, локальный CI-цикл `spec-validate` выполнился для всех 15. Повторное короткое ревью ожидается.
- Повторное независимое ревью двух P2: REVIEW_GO. Исходный пакет зафиксирован коммитом `0fea30f50b42ee0fbb941ce064b8bf685bb87479`, рабочее дерево после него было чистым. `build-release.py` собрал кандидат из этого SHA; preview v0-2-14 → v0-2-15 показал `candidate_sha256=67a947d3284b1553089dbc52298ce08b4bcca070b9394bb5a1ac64dd9f06234f`. После смысловой сверки kit apply с тем же fingerprint passed; установленная CLI показала `global-status: passed`, active v0-2-15. Временные права на чтение кандидата выданы только пользовательской учётной записи для этой установки.
- Документальный коммит `632c642c872261e2344b152bc17caf49b3991385` добавил результат локальной активации. Обычный `git push origin main` отправил диапазон `2d7ddd1..632c642`; удалённый `refs/heads/main` равен `632c642`. CI [36127281151](https://github.com/Lainterus1/HighGrade/actions/runs/36127281151) запущен и ещё не завершился при первом чтении.
- CI 36127281151 для опубликованного `632c642` завершился success. Финальная документальная запись фиксирует этот исход; код и установленная Поставка по-прежнему происходят из `0fea30f`.

- Инвентаризация: VERIFIED; 15 изменений, из них только HG-0018 имеет technical=ready. Для остальных `spec-check` сообщает пропущенные/устаревшие проверки, доказательства или ревью.
- Разрешение: пользователь явно разрешил локальный агентный пилот с передачей процедур Поставки и синтетических проектов; реальные проекты исключены.
- Локальные проверки: spec-run HG-0002 (10), HG-0003 (2), HG-0004 (4) прошли до уточнения тестов; изолированный синтетический жизненный цикл CLI завершился успешно и записан в `docs/evidence/spec-catalog/isolated-pilot-refresh.json`.
- Ревью: независимый агент нашёл два пробела в проверках сценариев HG-0002/HG-0003 и неполные списки входов HG-0008/HG-0012/HG-0015. Тесты HG-0002/HG-0003 уточняются; прежний GO к ним не применяется.
- После исправления тестов HG-0002 и HG-0003 получили свежее независимое REVIEW_GO, spec-review и spec-check passed. HG-0004 получила воспроизводимый изолированный CLI-пилот (20 операций, сборка точного бинарника, temp вне репозитория), независимое REVIEW_GO и spec-check passed.
- Разрешённый агентный пилот: s1 исправил ссылку прямым work без спеки; s3 оформил новый контракт до изменения кода и интегрировал его после тестов/ревью; s5 остановился на существенном вопросе о правиле округления. s2 запущен. Итоги нужно закрепить как проверяемые артефакты; это не подтверждает остальные процессные сценарии.
- Дополнительные пробы: s2 выполнил короткий рефакторинг прямым work (он не доказывает маршрут зависимых этапов); s4 исправил регрессию по неизменным spec.json/требованию, обновил результаты, сохранил human=stale; s6 исправил потерю данных и получил независимое REVIEW_GO после 3 тестов. Седьмой синтетический проект s7 проходит init.
- Общая проверка текущего дерева: `cargo nextest run --locked --profile highgrade` 116/116; `source-scenarios check` 50 требований, 89 сценариев, 38 автоматических; цепочка list → nextest → prepare → trace → verify завершилась, verify: 38 automatic_passed и 51 unconfirmed в структурном trace. `node scripts/check-repository.mjs` passed.
- Текущий срез `spec-list`: HG-0002/0003/0004/0006/0008/0012/0014/0015/0018 technical=ready; остальные шесть in_progress. HG-0006 S1–S7 получили агентные пробы s1–s7, независимое GO, spec-review, spec-check и spec-integrate. HG-0014 S1/S2 получили отдельное независимое GO и spec-check.
- HG-0016: первый перенос итоговых файлов в S1/S3 независимое ревью отвергло, поэтому их статус был исправлен на unknown. Повторные пилоты s1 и s5 сохранили исходное разрешение и один отправленный раунд двух вопросов; новые доказательства записаны, независимое ревью спецификации ещё идёт. HG-0017: в s3 добавлены сценарные ID к тестам, повторены 3/3 и отдельно воспроизведён отсутствующий отчёт с unconfirmed; независимое ревью ещё требуется.
- Для HG-0013-S5 создана изолированная установка Поставки v0-2-14 и сохранён незавершённый синтетический HG-0001 в s7. Ревью прежнего init s7 отвергло прямое присвоение PASS сценариям HG-0009 без сырого журнала и отдельных исходных условий.
- HG-0016 S1/S2/S3 после повторных агентных проб получили независимое GO, spec-review и spec-check passed. В s7 отдельный агент продолжил HG-0001 через установленную Поставку: красный тест 4!=2, затем 3/3, независимое GO, spec-check passed, интеграция; человеческая приёмка осталась pending. HG-0017-S1 и HG-0010-S9 записаны с подтверждёнными входами. HG-0017-S2 получил отдельное независимое GO после проверки структурного trace. По HG-0013-S5 сохранён полный аудит 38 запросов инструментам и независимое GO в границе этого журнала; после отдельного ревью всей HG-0013 записаны spec-review GO и spec-check passed.
- HG-0017-S3: синтетический s7 с установленной Поставкой дважды вернул EvidenceMissingOrStale при отсутствующем отчёте и technical=in_progress; агент не заявил PASS, после побайтового восстановления отчёта статус вернулся к ready. Независимое GO по S3 и всей HG-0017, spec-review и spec-check passed.
- HG-0010-S7 подтверждён независимым ревью по уже сохранённому агентному `s7-continuation`: native_report записан без ложного trace PASS. Проба `hg10-native-s3` для S7/S8 получила NO_GO: она не проходила маршрут агента; для S8 прежнее действующее PASS не становится пробелом от последующего skipped. Повтор S8 требуется на устаревшем входе.
- Повторный `highgrade-init` в синтетическом s6 сохранил исходные SHA кода/тестов/необычной заметки, создал канонические документы и реестр, прошёл 3/3 теста, doctor и оставил inspect=unknown по несогласованным бюджетам. S12 нельзя подтверждать, потому что путь к заметке был подсказан агенту.
- Независимое ревью подтвердило HG-0009-S6/S13/S17; записи evidence внесены. S12 остаётся NO_GO из-за подсказанного пути. По синтетическому s7 отдельный агент выполнил work→approve с точным расширением scope, 3/3 тестами и одним локальным коммитом только `docs/overview.md`; независимое ревью подтвердило HG-0011-S1/S3/S4, доказательства внесены. Местная активация там неприменима, push не было.
- Публикация: PENDING; известная цель `origin/main`, HEAD 2d7ddd1. Коммит, активация и push не выполнены.
- HG-0010-S6: на s6 добавлен один тест настоящего `store.save` с невалидным JSON-значением, 1/1 и 4/4 прошли. Независимое GO подтвердило raw вызовы агента и границу раннего отказа; evidence записано.
- HG-0010-S14/S16: локальный update-пилот на двух изолированных профилях получил независимое GO, доказательства записаны. S15 совпадающий состав проверен, отличающийся состав ещё открыт и проверяется отдельно. Реальный профиль не затронут.
- HG-0009-S12: слепой агентный поиск в s1 завершён; источник найден после bootstrap без подсказанного пути, тест 1/1, независимое ревью ещё идёт.
