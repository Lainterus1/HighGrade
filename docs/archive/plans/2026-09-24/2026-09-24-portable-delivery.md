# Portable delivery implementation

Version: 1. Approved in the conversation on 2026-09-24.

## Result and authority

Make the installed High Grade delivery self-contained, strengthen its existing SDD–BDD route, define project compatibility, verify the candidate in isolation, then repeat adoption in `D:/my_projects/Snaf_Website`.

Implementation, native checks, independent reviews, isolated installation and the SNAF pilot are authorized. The user confirmed that **all current uncommitted SNAF changes belong to the previous attempt and may be discarded**. Do not touch ignored runtime data, credentials or production. Commit, push, deployment and replacement of the normal user installation are outside this goal. Preserve the seven pre-existing terminology edits in High Grade.

## Sequential gates

| Stage | Authoritative change / result | Completion gate |
|---|---|---|
| P0 | Record starting state | Known roots, HEADs and change boundaries |
| P1 | `self-contained-delivery` | Shipped rules and adaptation contract work without source-repository documents; checks and review |
| P2 | `behavioral-delivery-workflow` | Meaningful scenarios, native evidence and resumable handoffs; checks and review |
| P3 | `delivery-project-compatibility` | Explicit supported schema, semantic compatibility procedure and non-destructive failure behavior; checks and review |
| P4 | Candidate qualification | Isolated installation and actual agent exercises on empty/existing fixtures; distinguish structural and behavioral evidence |
| P5 | Reset old SNAF attempt | Discard user-authorized tracked/untracked changes, preserve ignored data; verify baseline |
| P6 | Repeat SNAF adoption | Installed candidate drives adaptation and a bounded existing-behavior specification/test task; review and native gates |

Only one implementation change is active at a time. Each change owns its requirements and detailed tasks. Archive after its implementation checks and review; later pilot evidence may reveal defects to fix. P4/P6 are verification, not artificial product features. Do not implement SNAF backend or a new site feature.

## Final acceptance

- Each P1–P3 requirement has relevant evidence; gaps are not successes.
- Installed materials resolve within their own release and the target project.
- Agent exercises preserve foreign files, use native checks, retain accepted decisions and correctly report unavailable evidence.
- SNAF's former adoption is removed and the candidate is used for the new adoption; domain skills and existing product behavior are preserved.
- Candidate checks, local pilot, user acceptance and publication are reported separately.

State: [current checkpoint](2026-09-24-portable-delivery.state.md).
