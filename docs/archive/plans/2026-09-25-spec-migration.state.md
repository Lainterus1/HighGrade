# Переход Проекта на нативные требования

Источник задачи: согласованная задача в текущем сеансе; точечная сортировка выполняется через Goal.
Workspace: D:\my_projects\MyCodex
Observed HEAD: 2d7ddd1c8207a97a3a567011f009ff33182e15ba. Незакоммиченные правки HG-0006 сохраняются.
Status: DONE
Current: completed — локальная миграция и проверки завершены.
Next: none.
Blockers: none.

## Порядок

1. Зафиксировать исходники, ссылки, статусы и решение по каждому требованию.
2. Архивировать оригиналы с точными хешами и перенести необходимые действующие контракты в нативный формат без ложной приёмки.
3. Вывести технический учёт и заменённые правила из действующих требований; проверить судьбу HG-0005 и HG-0006 отдельно.
4. Переключить скрипты, CI, документацию и реестр на нативные источники; убрать рабочий OpenSpec.
5. Выполнить локальные проверки и независимое ревью; сохранить открытые пробелы честно.
6. После выявленного ограничения инструмента закрепить импортированные контракты как действующие без подмены доказательств сценариев: специализированный перенос с проверкой происхождения и ревью, затем повторный контроль каталога и CI.

Границы: не менять другие проекты, старые внешние форматы Поставки, глобальную установку, commit/push и человеческое решение. Агентный пилот исключён ранее автором; локальные проверки допустимы.

## Исходные наблюдения

7 действующих OpenSpec-файлов, 36 требований, 74 сценария. source-scenarios.mjs, check-scenario-scale.mjs и CI используют OpenSpec. CLI spec-import хранит путь и SHA256 источника и отвергает его отсутствие (ImportSourceDrift); оригиналы должны остаться по закреплённому архивному пути. HG-0006 остаётся неинтегрированной из-за отсутствия агентного пилота.

## Выполнение и доказательства

- 1 VERIFIED: [карта](../legacy-openspec/migration.json) распределяет все 36 требований и 74 сценария: 27 прямых импортов, 6 адаптаций, 3 исключения. Ревью выявило непокрытые гарантии HG-SS-R01…R05: они сохранены отдельными импортами. Два смысловых расхождения адаптаций исправлены; повторное независимое ревью дало GO для точности переноса.
- 2 VERIFIED для промежуточного создания черновиков: 86 исходных файлов побайтно сверены и перенесены в `docs/archive/legacy-openspec/`; 33 изменения созданы через `spec-import` с защищённым происхождением и прошли `spec-validate`. После этого они закреплены на этапе 6. `.gitattributes` защищает архивные байты на Windows.
- 3 VERIFIED: HG-0007 после независимого GO и успешного spec-check интегрирован; три технических требования HG-0005 выведены из действующего каталога. История HG-0005, тесты и проектные документы сохранены.
- 4 VERIFIED: рабочий `openspec/` удалён после сверки всех 86 хешей и проверки отсутствия ссылок/junction. Реестр, документация, CI и скрипты используют нативный каталог; OpenSpec CLI из CI исключён. Совместимое чтение прежнего формата в Поставке оставлено. `inspect` не обнаружил `failed`/`unknown` и превышений бюджетов; остались обычные предупреждения о внешних ссылках.
- 5 VERIFIED как промежуточный снимок до HG-0008 и переноса: nextest 113/113, trace 50 требований/90 сценариев, 25 подтверждённых автоматических связей и 65 неподтверждённых. Итоговые числа и проверки после изменения — в этапе 6.

Решение ограничения: обычный `spec-integrate` требует доказательств выполнения сценариев, поэтому прежние контракты закреплены отдельным `spec-transfer` без выдуманного PASS. Техническая готовность и человеческая приёмка остались неподтверждёнными. Внешний агентный пилот автор исключил.

## Этап 6 — закрепление прежних контрактов

- HG-0008 добавил узкий `spec-transfer`: только импортированные add-операции с совпадающими исходным хешем, ID, завершённой сверкой и свежим независимым GO. Обычный `spec-integrate` по-прежнему требует настоящих доказательств. Проверки legacy store и каталога v3 покрывают успех, дрейф источника, устаревшее ревью, смешанные операции, ошибочный ID и отсутствие доказательств; отчёт — `specs/changes/HG-0008/evidence/transfer-tests.txt`. Спека HG-0008 прошла `spec-check` и интегрирована; после усиления теста evidence и ревью обновлены, `spec-check` снова passed.
- Независимое ревью дало GO по 33 контрактам: 27 прямых, 6 согласованных адаптаций; 86 архивных хешей, 69 сценарных ID и полнота распределения 36 исходных требований сверены. CLI закрепил все 33 в `specs/requirements/`, записал origins и архивировал изменения. `spec-list`: 48 сохранённых требований, 33 origins; все 33 переноса `technical=in_progress`, `human=pending`. Выборочный `spec-check HG-IMP-PD-R01` возвращает ожидаемый `EvidenceMissingOrStale`. Карта переноса теперь отмечает `native_transferred_behavior_unverified`.
- Итоговая локальная проверка после изменений: nextest 115/115, 0 skipped; `source-scenarios check/prepare/verify` passed, 51 эффективное требование (включая 3 открытых HG-0006), 92 сценария, 25 подтверждённых автоматических связей и 67 неподтверждённых сценариев. Масштаб 100/200, отрицательные duplicate/unlinked, 7 Python-тестов, `cargo fmt --check`, 26/26 хешей kit, `check-repository --verify-import`, `doctor`, изолированная установка и `global-status` passed. Архив: 86/86 хешей; каталога `openspec/` больше нет. Реальный профиль, публикация, пилот и человеческая приёмка не выполнялись.
- `inspect` первоначально обнаружил `BudgetExceeded`: сырой `specs/catalog.json` вырос до 68 714 байт, `docs/DEVELOPMENT.md` — до 8424. Убрано дублирование в DEVELOPMENT (7613 байт), действующий каталог оставлен зарегистрированным, но исключён из обычной загрузки; выборка идёт через `spec-list/read`. Итоговый `inspect`: маршрут 80 947 из 94 000 байт, без `failed`/`unknown` и превышений; остались `ExternalLinkUnchecked`.
- При разработке нового CLI Cargo однажды повторно использовал прежний бинарник. `cargo clean -p highgrade` удалил только артефакты пакета; последующие тесты и CLI пересобраны из текущего исходника. Это было окружение сборки, не результат теста поведения.
- Финальное независимое ревью дало GO при условии переснять отчёт после последней редакции карты. Условие закрыто штатной цепочкой `nextest → prepare → trace → verify` на итоговом снимке: 115/115 passed, 25 автоматических связей passed, 67 сценариев явно unconfirmed. Не подтверждались агентный пилот, удалённый CI, публикация, активация и человеческая приёмка.
