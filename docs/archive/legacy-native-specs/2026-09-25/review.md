# Независимое локальное ревью канонизации

25.09.2026 субагент выполнил ревью действующих требований, архивной карты, тестовых связей и документов. Он не изменял файлы, не использовал внешний `codex review` и не проводил агентный пилот.

Первое ревью дало `REVIEW_NO_GO` по двум замечаниям:

1. Гарантия единственного владельца после выборочного переноса и сохранения соседнего прежнего требования была ошибочно снята вместе с переходным правилом. Она сохранена в `HG-0008-R1/S3`; тест `reviewed_transfer_trace_selects_one_owner_and_preserves_neighbor` проходит импорт, ревью, перенос и trace, затем проверяет оба сценария и отсутствие `DuplicateScenarioId`/failed findings.
2. `kit/procedures/clear.md` безусловно требовал OpenSpec. Теперь он обращается к нему только в проекте, явно сохранившем старый маршрут; хеш поставляемого файла обновлён в манифесте.

Промежуточное предположение о повторном перезаписывании через `spec-transfer` было опровергнуто: `change_mut` отвергает архивное изменение до переноса. Регрессионный тест закрепляет отказ без изменения актуального требования. Проверка сценарных маркеров также усилена: она отвергает каждую неверную и лишнюю метку и пропажу метки у настроенного автоматического теста.

Повторное ревью после исправлений: **`REVIEW_GO`**, новых ошибок не найдено. Его пределы: это локальная проверка исходников и отчётов, не испытание агента в подключённом проекте и не решение человека о приёмке.
