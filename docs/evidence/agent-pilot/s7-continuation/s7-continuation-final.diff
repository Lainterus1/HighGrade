--- before/README.md
+++ after/README.md
@@ -1,6 +1,6 @@
 # Счётчик заказов
 
-Учебный проект считает заказы: `count_orders` получает список заказов и возвращает их количество. Пустой список даёт `0`. Серверного развёртывания у примера нет.
+Учебный проект считает уникальные заказы: `count_orders` получает список строковых ID и возвращает число различных ID. Повтор одного ID учитывается один раз, пустой список даёт `0`. Серверного развёртывания у примера нет.
 
 Документы проекта: [правила агента](AGENTS.md), [устройство](docs/ARCHITECTURE.md), [инженерные соглашения](docs/ENGINEERING.md), [подготовка и проверки](docs/DEVELOPMENT.md). [Обзор](docs/overview.md) указывает на разделённые документы. Отдельная [инструкция по передаче](docs/deployment-guide.md) сохранена.
 
--- before/counter.py
+++ after/counter.py
@@ -1,2 +1,2 @@
 def count_orders(orders: list[str]) -> int:
-    return len(orders)
+    return len(set(orders))
--- before/test_counter.py
+++ after/test_counter.py
@@ -9,3 +9,8 @@
 
     def test_two(self):
         self.assertEqual(count_orders(["a", "b"]), 2)
+
+    def test_unique_order_ids(self):
+        """HG-0001-S1: повторный ID обозначает тот же заказ."""
+        self.assertEqual(count_orders(["a", "b", "a", "a"]), 2)
+        self.assertEqual(count_orders(["a", "a"]), 1)
--- before/docs/ARCHITECTURE.md
+++ after/docs/ARCHITECTURE.md
@@ -1,5 +1,5 @@
 # Действующее устройство
 
-`counter.py` содержит функцию `count_orders`. Она получает список заказов и возвращает длину списка. Пустой список даёт `0`. Слой хранения в этой учебной системе отсутствует.
+`counter.py` содержит функцию `count_orders`. Она получает список строковых ID заказов и возвращает число различных ID. Повторы учитываются один раз, пустой список даёт `0`. Слой хранения в этой учебной системе отсутствует.
 
 Инвариант неотрицательного результата закреплён в [правилах проекта](../AGENTS.md) и применяется при изменениях по [инженерным соглашениям](ENGINEERING.md). Тесты находятся в `test_counter.py`; команда запуска приведена в [документе разработки](DEVELOPMENT.md). Каталог изменений требований — `../specs/`.
--- before/docs/DEVELOPMENT.md
+++ after/docs/DEVELOPMENT.md
@@ -8,6 +8,6 @@
 python -m unittest discover
 ```
 
-Ожидаемый результат для текущего `test_counter.py` — два успешных теста: пустой список возвращает `0`, список из двух заказов возвращает `2`. Изменения поведения проверяются в этом файле; инвариант результата приведён в [инженерных соглашениях](ENGINEERING.md). Независимая [инструкция по передаче](deployment-guide.md) описывает передачу исходных файлов другому разработчику.
+Ожидаемый результат для текущего `test_counter.py` — три успешных теста: пустой список возвращает `0`, два разных ID дают `2`, а повторный ID не увеличивает счёт. Изменения поведения проверяются в этом файле; инвариант результата приведён в [инженерных соглашениях](ENGINEERING.md). Независимая [инструкция по передаче](deployment-guide.md) описывает передачу исходных файлов другому разработчику.
 
 Каталог спецификаций High Grade — `../specs/`. При работе с требованиями используй `spec-list --root <абсолютный путь к проекту>` и выборочный `spec-read`. Штатный тест запускается напрямую приведённой командой; раннер High Grade для него не настроен.
--- before/specs/changes/HG-0001/spec.json
+++ after/specs/changes/HG-0001/spec.json
@@ -40,7 +40,7 @@
     },
     {
       "description": "Реализовать правило и добавить регрессионный тест",
-      "done": false,
+      "done": true,
       "id": "HG-0001-T2"
     },
     {
--- before/specs/changes/HG-0001/results.json
+++ after/specs/changes/HG-0001/results.json
@@ -0,0 +1,29 @@
+{
+  "acceptance": [],
+  "evidence": {
+    "HG-0001-S1": {
+      "captured_at": "2026-09-25T08:31:48Z",
+      "command": "python -B -m unittest -v",
+      "files": {
+        "counter.py": "e5c5552f882bee9c85f79ca444260f94d514cf54b034a72463ec22c3d918c4ac",
+        "evidence/s7-continuation-unittest-verbose.txt": "f117d681b13e52df998e542ca7bd8e52b7ac0c0afbe87588c5255086899bf3af",
+        "test_counter.py": "0d57577fc8ffb490a46fffca29e20690e072d2d40e20bb0589062a4715be3615"
+      },
+      "method": "native_report",
+      "observation": "Выполнены 3 теста: пустой список дает 0, разные ID дают 2, повторные ID дают 2 и 1; тест test_unique_order_ids помечен HG-0001-S1. До исправления этот тест падал: 4 вместо 2.",
+      "outcome": "passed",
+      "report": "evidence/s7-continuation-unittest-verbose.txt",
+      "scenario": "HG-0001-S1",
+      "scenario_sha256": "a9fd1e408f2fe69edf4b352c6f6984ffda3da3ac2b72faba5a72efc64cfcda82"
+    }
+  },
+  "history": [
+    {
+      "evidence": {},
+      "recorded_at": 1790325189,
+      "review": null
+    }
+  ],
+  "review": null,
+  "runs": []
+}