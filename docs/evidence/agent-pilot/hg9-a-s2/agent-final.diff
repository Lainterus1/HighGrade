--- before/AGENTS.md
+++ after/AGENTS.md
@@ -1,3 +1,5 @@
 # Правила проекта
 
 Пользуйся процедурами High Grade. Язык проекта — русский. Запускай тесты и не публикуй изменения.
+
+Местные источники, команда проверки и открытые полномочия — в [проектной инструкции](.highgrade/project/INSTRUCTIONS.md).
--- before/README.md
+++ after/README.md
@@ -2,4 +2,4 @@
 
 `quote.py` и `receipt.py` вычисляют сумму после скидки. Это действующее поведение; результаты для существующих входов должны сохраняться.
 
-Устройство, правила правок и команды пока находятся в [старом руководстве](docs/project-guide.md). Подробный выбор тестов — в [стратегии проверок](docs/test-strategy.md).
+Местные источники: [правила агента](AGENTS.md), [устройство](docs/ARCHITECTURE.md), [правила правок](docs/ENGINEERING.md) и [команды](docs/DEVELOPMENT.md). [Прежнее руководство](docs/project-guide.md) сохраняет карту перенесённых разделов; подробный выбор тестов — в [стратегии проверок](docs/test-strategy.md).
--- before/docs/project-guide.md
+++ after/docs/project-guide.md
@@ -1,15 +1,9 @@
-# Руководство по проекту расчёта заказа
+# Карта прежнего руководства
 
-## Устройство
+Содержание этого смешанного руководства разнесено по действующим документам без изменения расчёта заказа:
 
-`order_loader.py` читает `orders.json` и собирает предметный объект из `order_model.py`. `discount.py` вычисляет скидку, `quote.py` возвращает предварительную сумму, а `receipt.py` формирует итоговый чек. `order_report.py` собирает сводку. Действующий инвариант: для существующих входов сумма после скидки не меняется от организационной перестройки документов.
+- устройство модулей и данных — в [архитектуре](ARCHITECTURE.md);
+- правило правок — в [инженерных соглашениях](ENGINEERING.md);
+- команда и предел проверок — в [документе разработки](DEVELOPMENT.md).
 
-## Правила разработки
-
-Менять расчёт скидки следует вместе с проверкой сумм и чека. Сохранять значения исходных входов; если правило меняется, сначала сформулировать новый сценарий и затем обновить тест. Данные `orders.json` — синтетическая локальная фикстура.
-
-## Команды
-
-Запуск проверок: `python -B -m unittest discover -v`. Отдельной сборки и развёртывания у проекта нет; внешний CI не настроен.
-
-Профильная [стратегия проверок](test-strategy.md) уточняет выбор сценариев и остаётся самостоятельным документом.
+Отдельная [стратегия проверок](test-strategy.md) по-прежнему выбирает сценарии. Действующее правило сохранения результатов для существующих входов находится в [README](../README.md). Этот файл оставлен для входящих ссылок и истории переноса; он больше не владеет правилами или командами.
--- before/docs/test-strategy.md
+++ after/docs/test-strategy.md
@@ -2,4 +2,4 @@
 
 Этот документ отвечает за выбор проверок, а не за команды запуска или устройство модулей. Для изменений суммы проверять как итог `quote.py`/`receipt.py`, так и исходные позиции заказа. Для рефакторинга без смены поведения сначала сохранять тесты на существующих входах. Внешних интеграций в синтетической фикстуре нет.
 
-Штатная команда находится в основном документе команд после адаптации. Не приписывать локальным тестам проверку реальных оплат или доставки.
+Штатная команда находится в [документе разработки](DEVELOPMENT.md). Не приписывать локальным тестам проверку реальных оплат или доставки.
--- before/docs/ARCHITECTURE.md
+++ after/docs/ARCHITECTURE.md
@@ -0,0 +1,7 @@
+# Устройство расчёта заказа
+
+`discount.py` вычисляет целочисленную сумму после скидки как `sum(prices) * (100 - discount_percent) // 100`. `quote.py` и `receipt.py` вызывают эту функцию и предоставляют `quote_total` и `receipt_total`. Действующее требование сохранять их результаты для существующих входов установлено в [README](../README.md).
+
+`order_loader.py` читает локальный JSON и создаёт неизменяемые объекты `Order` из `order_model.py`. Поле `prices` — кортеж; `order_report.py` принимает путь и возвращает список словарей с `id`, `quote`, `receipt`. [orders.json](../orders.json) — синтетическая входная фикстура. Эти связи сверены с кодом и действующими тестами, а не выведены из имени файла.
+
+Предыдущее смешанное описание сохранено как [карта переноса](project-guide.md). Правила изменения и проверок находятся в [инженерных соглашениях](ENGINEERING.md) и [документе разработки](DEVELOPMENT.md).
--- before/docs/ENGINEERING.md
+++ after/docs/ENGINEERING.md
@@ -0,0 +1,5 @@
+# Инженерные соглашения
+
+При изменении расчёта скидки соблюдай [действующее правило результата](../README.md) и выполняй проверки по [профильной стратегии](test-strategy.md). Если правило должно измениться, сначала сформулируй новый сценарий, затем измени тест и реализацию через согласованный маршрут спецификации.
+
+Профильная стратегия владеет выбором сценариев, включая перестройку без смены поведения; здесь закреплён маршрут изменения. Команда запуска — в [документе разработки](DEVELOPMENT.md), устройство и локальные данные — в [архитектуре](ARCHITECTURE.md).
--- before/docs/DEVELOPMENT.md
+++ after/docs/DEVELOPMENT.md
@@ -0,0 +1,9 @@
+# Локальные команды
+
+Штатная проверка из корня проекта требует Python со стандартным `unittest`:
+
+```powershell
+python -B -m unittest discover -v
+```
+
+Предыдущее выполнение семи тестов записано в [исходном отчёте](../test-results.txt) и [завершённом плане](../PLAN.md); текущий пилот сохраняет отдельный новый вывод в `evidence/hg9-a/`. Отдельной сборки и развёртывания в обследованном проекте нет; прежнее руководство сообщает, что внешний CI не настроен. Локальные тесты не проверяют реальные оплаты и доставку; [стратегия проверок](test-strategy.md) определяет выбор сценариев.
--- before/.highgrade/project/INSTRUCTIONS.md
+++ after/.highgrade/project/INSTRUCTIONS.md
@@ -0,0 +1,26 @@
+---
+highgrade_project_schema: 1
+highgrade_spec_format: native-v1
+---
+
+# Проектная инструкция High Grade
+
+Это локальное подключение синтетического расчёта заказа. Язык авторских документов, планов и отчётов — русский по [правилам проекта](../../AGENTS.md). [README](../../README.md) владеет назначением и действующим правилом результата для существующих входов.
+
+## Действующие источники
+
+- [Архитектура](../../docs/ARCHITECTURE.md) описывает модули, модель и данные; [инженерные соглашения](../../docs/ENGINEERING.md) — правило изменений; [разработка](../../docs/DEVELOPMENT.md) — команду и границы проверки.
+- [Стратегия проверок](../../docs/test-strategy.md) отдельно владеет выбором сценариев. [Карта прежнего руководства](../../docs/project-guide.md) сохраняет входящие ссылки после разделения его трёх разделов и не является вторым владельцем правил.
+- [PLAN.md](../../PLAN.md) и [test-results.txt](../../test-results.txt) — история завершённой технической перестройки, не действующее новое требование. [Реестр](documents.json) задаёт роли, области чтения и бюджеты.
+
+## Маршрут и проверки
+
+- Новые изменения поведения оформляй в проектном каталоге [specs/](../../specs/README.md) в нативном JSON High Grade. Прежнего хранилища спецификаций в обследованной фикстуре нет; завершённый PLAN не мигрируется в новое требование. Исправление по неизменному контракту выполняй прямой технической работой с проверками по установленной процедуре task.
+- Штатный запуск: `python -B -m unittest discover -v` из корня проекта. Проектный runner для `spec-run` не настроен; запуск прямой. Проверяй оба расчёта и отчёт по [профильной стратегии](../../docs/test-strategy.md). Структурные `doctor` и `inspect` не подтверждают поведение.
+- Пересекающихся местных навыков в обследованном списке файлов нет. Теги и дополнительные runner не согласованы, поэтому не созданы.
+
+## Полномочия и неизвестное
+
+- Для этого пилота разрешены локальная адаптация s2 и проверки. Коммит, публикация, глобальное переключение и активация не разрешены. Общее право на будущие коммиты, пользовательская приёмка и её условия неизвестны; локальные тесты не являются такой приёмкой.
+- Бюджеты документов и маршрута не согласованы и остаются `null` в [реестре](documents.json). Это открытое решение, а не успешная проверка размера.
+- Внешняя эксплуатация и способ её активации не установлены. Прежнее руководство сообщает об отсутствии отдельной сборки, развёртывания и внешнего CI в обследованной фикстуре; это не утверждение о системах вне неё.
--- before/.highgrade/project/documents.json
+++ after/.highgrade/project/documents.json
@@ -0,0 +1,19 @@
+{
+  "schema_version": 1,
+  "documents": [
+    {"id":"readme","path":"README.md","role":"purpose-navigation","loading":"entry","scope":[],"budget":null},
+    {"id":"agents","path":"AGENTS.md","role":"agent-rules","loading":"entry","scope":[],"budget":null},
+    {"id":"architecture","path":"docs/ARCHITECTURE.md","role":"current-architecture","loading":"task","scope":["discount.py","quote.py","receipt.py","order_model.py","order_loader.py","order_report.py","orders.json"],"budget":null},
+    {"id":"engineering","path":"docs/ENGINEERING.md","role":"engineering-rules","loading":"task","scope":["test_totals.py","test_order_report.py"],"budget":null},
+    {"id":"development","path":"docs/DEVELOPMENT.md","role":"commands-procedures","loading":"task","scope":["test-results.txt"],"budget":null}
+  ],
+  "additional_sources": [
+    {"path":".highgrade/project/INSTRUCTIONS.md","role":"project-adaptation","active":true,"loading":"entry","scope":[".highgrade/project/documents.json"]},
+    {"path":"docs/test-strategy.md","role":"test-selection","active":true,"loading":"task","scope":[]},
+    {"path":"docs/project-guide.md","role":"legacy-navigation","active":true,"loading":"history","scope":[]},
+    {"path":"PLAN.md","role":"completed-technical-plan","active":true,"loading":"history","scope":[]},
+    {"path":"specs/README.md","role":"specification-store","active":true,"loading":"task","scope":["specs"]},
+    {"path":"evidence/hg9-a/agent-audit-plan.md","role":"pilot-evidence","active":true,"loading":"history","scope":["evidence"]}
+  ],
+  "route_budget": null
+}
--- before/specs/README.md
+++ after/specs/README.md
@@ -0,0 +1,7 @@
+# Спецификации проекта
+
+Данные принадлежат проекту и сохраняются при удалении High Grade.
+`catalog.json` — версия и счётчик; `requirements/` — действующие требования;
+`changes/<ID>/spec.json` — изменение, `results.json` — проверки и решения.
+Записывайте через инструмент с проверкой ожидаемого хеша. При наличии
+`transaction.json` чтение заблокировано до явного `spec-recover`.
--- before/specs/catalog.json
+++ after/specs/catalog.json
@@ -0,0 +1,7 @@
+{
+  "schema_version": 3,
+  "next_number": 1,
+  "retired_ids": [],
+  "origins": {},
+  "legacy_sha256": null
+}--- before/specs/runners.json
+++ after/specs/runners.json
@@ -0,0 +1 @@
+{}--- before/specs/tags.json
+++ after/specs/tags.json
@@ -0,0 +1 @@
+{}