# Локальная проба HG-0010-S6

Добавлен тест `test_invalid_data_preserves_existing_file`: он создаёт прежний `settings.json` с намеренно отличающимся форматированием, вызывает настоящий `store.save(path, {"theme": object()})`, ожидает `TypeError`, сравнивает байты файла с исходными и проверяет отсутствие лишних файлов. Для этого используется только временный каталог; `store.save`, сериализация и файловые операции не подменялись. Тест прошёл, дефект реализации не обнаружен, `store.py` не менялся.

## Порядок действий и сырые результаты

1. Прочитаны `setup.json`, снимки `before/`, проектные `AGENTS.md`, `README.md`, инструкция, `store.py`, `test_store.py` и относящиеся к сохранности документы. SHA-256 всех восьми входов совпали с `setup.json` и `before/`.
2. Прочитаны установленные `highgrade-work/SKILL.md`, `rules.md` и `procedures/work.md` из профиля `s7/.pilot-profile`. Установленным CLI v0-2-14 вызваны `global-status --profile <s7/.pilot-profile>` и `doctor --root <s6>`; оба вернули код `0`, статус `passed`: [сырой global-status](global-status.json), [сырой doctor](doctor.json). Эти проверки подтверждают только совместимость маршрута, не поведение `save`.
3. В `test_store.py` добавлен тест невалидного ввода. Выполнено `python -B -m unittest -v test_store.StoreTests.test_invalid_data_preserves_existing_file`: код выхода `0`, один выполненный тест `ok`, [сырой вывод](targeted-unittest.txt). Отказ и побайтовая сохранность проверены утверждениями теста.
4. Обновлены `docs/ENGINEERING.md` и `docs/DEVELOPMENT.md` с фактическим четвёртым тестом. Выполнена штатная команда `python -B -m unittest -v`: код выхода `0`, четыре теста `ok`, [сырой вывод](full-unittest.txt).
5. [Машинный снимок SHA-256](sha-manifest.json) повторно сверил все исходные файлы; изменены только `test_store.py` и два связанных документа. [Diff](changes.diff) показывает точные правки. Исходник `store.py` сохранил SHA-256 `db8138129281e5e02929ff01ecaa1e9d8f7cf1ec4d082827da09322ffe17115f`.

Граница: проверено локальное поведение Python при несериализуемом значении в изолированном временном каталоге. Внешний сервис, сбой питания и другие неиспытанные виды ошибок не подтверждены этим новым тестом. Формальный PASS, независимое ревью, коммит и публикация не выполнялись.
