# Независимая проверка обновления Поставки

Вердикт: **REVIEW_GO** для выполненных веток HG-0010-S14, S15 и S16. Существенных дефектов не найдено.

S14: preview и apply использовали один `candidate_sha256`; установленная CLI подтвердила v0-2-14. Git SHA `2bf93b6` получен из входного setup; пилот независимо проверил байты подготовленного пакета и CLI, но не происхождение архива через Git.

S15: при совпадающем составе `GlobalSameRelease` не привёл к apply, манифест и CLI совпали, снимок профиля остался прежним. Дополнительная проба с тем же ID и валидным пакетом, отличающимся только `rules.md`, также вернула `GlobalSameRelease`. Агент сравнил SHA манифеста с установленным журналом, назвал конфликт ID и не объявил «уже установлено». Независимый рецензент пересчитал все записи манифеста и подтвердил отсутствие apply и неизменность 32 файлов профиля. Ветка невозможности сравнения не исполнялась.

S16: повреждённый пакет отклонён на предварительной проверке с `GlobalManifestHashMismatch`; прежняя CLI сразу подтвердила v0-2-13, все 32 файла профиля побайтово сохранены. Сбой после переключения, rollback и недоступная диагностика не исполнялись.

Сырые вызовы агента в локальном rollout согласуются с журналом, JSON и снимками; SHA доказательств совпадают. Проверка ограничена изолированными профилями. Реальная установка пользователя и проекты не затрагивались.
