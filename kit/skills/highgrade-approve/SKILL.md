---
name: highgrade-approve
description: 'Принять проверенную задачу локально: точный коммит и предусмотренная проектом локальная активация из его SHA.'
---

<!-- Generated by scripts/render-skills.py from procedures/approve.md. -->

Профиль определяется по расположению `<профиль>/.agents/skills/highgrade-approve/SKILL.md`; корень проекта — по workspace пользователя. Активный выпуск указан в `<профиль>/.highgrade/global/active.json`; его CLI и материалы находятся в `<профиль>/.highgrade/global/releases/<release>/`. На первом входе прочитай `rules.md` выпуска, AGENTS и местную инструкцию проекта; проверь `global-status --profile <профиль>` CLI этого выпуска. Переиспользуй проверенный контекст по rules.md. Отсутствие проектной инструкции при init ожидаемо. При повреждении установки назови фактическую ошибку. Ниже уже находится инструкция этапа; `procedures/approve.md` повторно читать не нужно. Все указанные пути материалов относятся к корню активного выпуска.

# Локальное принятие задачи

## Решение пользователя

Явное «принимаю и активируй» или «активируй» в ответ на показанный точный готовый результат может одним действием разрешать его приёмку и активацию. Перед записью решения должны быть известны выбранные спеки, редакция, проверки и существенный остаток. Запиши основание из сообщения пользователя через spec-decide для этих отпечатков; повторный вопрос не нужен. Предварительное «выполняй с принятием и активацией» разрешает работу, коммит и активацию, но не означает человеческую приёмку ещё не показанного результата. Пробная активация и просьба только установить также не означают приёмки.

Решение принять не подтверждает выполнение сценария, требующего наблюдения человека. Такое наблюдение записывается только по фактическим сведениям пользователя. Приёмка, коммит и фактическая активация сохраняют отдельные статусы: ошибка активации не отменяет уже данное решение или созданный коммит.

Запускай по явному вызову `highgrade-approve` для конкретной задачи. Старый `highgrade-deploy` не является маршрутом активной поставки после миграции; его вызов не разрешает заменять полномочия двух новых навыков. Вызов approve разрешает локальный коммит и только описанную проектом локальную активацию из этого коммита. Push, PR, тег и эксплуатационное развёртывание не разрешены. Уже данное разрешение не переспрашивай.

Установи основной источник задачи, состояние спецификации, если она требуется по маршруту task, проверки, ревью, документы и точный состав файлов и участков. Для нативного change используй `spec-check --brief true` как краткую передачу уже записанного результата; полную историю читай только при конкретном пробеле. `work` готовит содержание, доказательства и архив; approve сверяет их актуальность для принятого состава и устраняет лишь выявленные пробелы. Неясное и постороннее оставь вне коммита. Причины отвергнутого сохрани в принятом архиве, одноразовое удаляй только после проверки принадлежности и ссылок. Не переписывай чужой индекс Git и не применяй без разбора `git add -A`, `reset` или `stash`.

Перед коммитом сверяй обязательные доказательства с выбранным индексом по диагностике передачи (`references/tools/diagnostics.md#состав-передачи-доказательств` внутри активного выпуска); учти ignored отчёты и невосстановимые входы, не добавляй все логи автоматически. Внешний маршрут проверяется отдельно по местным правилам.

Проверь полный staged diff, происхождение, лицензии, секреты и кэши. Используй результаты work, если состав и значимые зависимости не изменились; запускай заново лишь затронутую проверку, пробел или обязательный барьер проекта. Для технической задачи без спеки проверь поручение или план, результат и доказательства; не требуй spec ID или spec-check. При наличии спеки заверши её задачу по фактическому состоянию; укажи её ID в сообщении коммита, если ID существует. До затратной сборки проверь полномочия, свободный release ID и что среда сборки передаст читаемый кандидат среде применения; при разных учётных записях используй согласованный общий контекст, не расширяй доступ ко всему профилю. Создай коммит выбранного состава и сообщи SHA. Если проектная инструкция не назначает локальную активацию, на этом этап закончен. Если назначает, проверь точный SHA и используй изолированную копию именно этого SHA и описанные preview, применение и проверку. При сбое установи фактическое состояние и следуй проектному способу восстановления в имеющихся полномочиях. Автоматический откат утверждай только при подтверждённой гарантии конкретного механизма; если способ неизвестен, останови зависимое действие и назови пробел. Не подставляй global-update вместо команды подключаемого проекта. Не активируй правки рабочего дерева. Неудача активации не отменяет коммит: сообщи «закоммичено, локальная активация не прошла», причину и следующий шаг; не объявляй задачу полностью локально активной.

Разделяй в отчёте выполненную спеку и проверки, коммит, местную активацию, удалённую публикацию и пользовательскую приёмку. Коммит одной задачи не требует известного маршрута push.

Каталог спецификаций, results и исходные доказательства входят в проектные данные выбранной задачи. Различай техническое ревью, локальный коммит и решение человека. После повторного прогона считай старое решение действующим только если spec-list подтверждает accepted для текущей редакции; самостоятельно не переносить acceptance на новые доказательства.
