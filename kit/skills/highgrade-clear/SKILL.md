---
name: highgrade-clear
description: 'По запросу проверить и актуализировать состояние проекта, документы, контекст, совместимость и архив; анализ остается только чтением.'
---

<!-- Generated by scripts/render-skills.py from procedures/clear.md. -->

Профиль определяется по расположению `<профиль>/.agents/skills/highgrade-clear/SKILL.md`; корень проекта — по workspace пользователя. Активный выпуск указан в `<профиль>/.highgrade/global/active.json`; его CLI и материалы находятся в `<профиль>/.highgrade/global/releases/<release>/`. На первом входе прочитай `rules.md` выпуска, AGENTS и местную инструкцию проекта; проверь `global-status --profile <профиль>` CLI этого выпуска. Переиспользуй проверенный контекст по rules.md. Отсутствие проектной инструкции при init ожидаемо. При повреждении установки назови фактическую ошибку. Ниже уже находится инструкция этапа; `procedures/clear.md` повторно читать не нужно. Все указанные пути материалов относятся к корню активного выпуска.

# Актуализация состояния проекта

Clear — обзор состояния, а не команда удаления. При запросе «проверь» или «проанализируй» работай только на чтение. Для неинициализированного проекта `inspect --bootstrap` показывает структуру документов; область обзора кода находи целевым чтением и аудитом. Используй общий аудит, `doctor`/`inspect` и применимые штатные проверки; сравни фактический код, действующие требования, владение документами, ссылки, бюджеты контекста, временные обходы, архив и обязанности навыков. OpenSpec проверяй только там, где проект явно сохранил его действующим маршрутом. Проверь схему и совместимость проектной инструкции с глобальной поставкой; назови противоречия и затронутые задачи.

Если исправления поручены, меняй только согласованную область, проверяй результат и сохраняй историю. Повышение бюджетов и удаление функциональности требуют отдельного существенного решения. Обычное обновление документов и архива не ждёт вызова clear.

Применяй контракт совместимости (`references/tools/diagnostics.md#совместимость-проекта` внутри активного выпуска): схема отдельно от смысла, принятые исключения сохраняются, автоматически проектная инструкция не переписывается.

Проверь каталог specs (или путь из specs-location.json), действующие требования, осиротевшие ссылки, актуальность доказательств и широких тегов. spec-list/spec-read не запускают проверки; повторный spec-run выполняй только в разрешённой области. Не переносить файлы по статусам и не удалять проектный каталог вместе с .highgrade. Не объединяй синонимы тегов без оценки контекста; согласованное объединение выполняет spec-tag-merge одной транзакцией.

Если в области обзора уже есть issues/, используй issue-list/read и выборочный issue-validate: проверь применимость решений, verification_current, ссылки на evidence и существенные открытые обходы. Ошибка или устаревание записи — находка, а не разрешение автоматически повторять рецепт. Отсутствующий реестр не создавай. Владение решением — по Fixer (`procedures/fix.md` внутри активного выпуска).
