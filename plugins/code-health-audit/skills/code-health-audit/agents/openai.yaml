interface:
  display_name: "Code Health Audit"
  short_description: "Deterministic read-only Python code health audit"
  default_prompt: "Use $code-health-audit to audit this Python repository without changing code."
